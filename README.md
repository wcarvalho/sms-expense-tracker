# SMS Expense Tracker

Install
```
brew install hugo npm
sudo npm install netlify-cli -g
sudo npm install
```

Testing netlify
```
npm run dev
sudo netlify dev --live
```

Serving netlify
```
sudo netlify deploy --prod
```